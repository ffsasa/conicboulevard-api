name: CI/CD Deploy ConicBoulevard-API

on:
  push:
    branches: [ "main" ]        # chạy khi push nhánh main
  workflow_dispatch:             # cho phép bấm nút Run workflow thủ công

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 🧾 Checkout source
      uses: actions/checkout@v4

    - name: ☕ Set up JDK 17 (for tests)
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'maven'

    - name: ✅ Run unit tests
      run: mvn -q -DskipTests clean package

    - name: 🔐 Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: 🏷️ Set image tags
      id: vars
      run: |
        SHA_TAG=${GITHUB_SHA::7}
        echo "SHA_TAG=$SHA_TAG" >> $GITHUB_OUTPUT
        echo "IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/${{ secrets.IMAGE_NAME || 'conicboulevard-api' }}" >> $GITHUB_OUTPUT

    - name: 🧱 Build & Push image (latest + sha)
      uses: docker/build-push-action@v6
      with:
        context: .
        push: true
        tags: |
          ${{ steps.vars.outputs.IMAGE }}:latest
          ${{ steps.vars.outputs.IMAGE }}:${{ steps.vars.outputs.SHA_TAG }}

    - name: 🚀 Deploy to VPS via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USER }}
        password: ${{ secrets.VPS_PASSWORD }}
        port: 22
        script: |
          cd ~/conicboulevard-api
          # Pull image mới (latest)
          docker-compose pull conicboulevard-api
          # Restart dịch vụ BE (không đụng tới DB)
          docker-compose up -d --no-deps conicboulevard-api
          # Dọn image cũ (tùy chọn)
          docker image prune -f
